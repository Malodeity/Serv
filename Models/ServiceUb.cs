using System;
using System.Net.Http;
using System.Text;
using Android.App;
using Android.Content;
using Newtonsoft.Json;
namespace UberEatsV1.Models
{
    public class ServiceUb
    {


       

        //public async void Update(Customer cust)
        //{
            //string url2 = @"http://10.0.2.2:8080/api/UpdateCust";
            //var json = JsonConvert.SerializeObject(cust);
            //var content = new StringContent(json, Encoding.UTF8, "application/json");

            //HttpResponseMessage response = null;
            //response = await client.PutAsync(url2, content);

            //if (response.IsSuccessStatusCode)
            //{
            //    GetCusts(cust.CustEmail, cust.CustPassword);
            //}
        }
    }
//}
